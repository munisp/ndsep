import { useState } from "react";
import { ActivityIndicator, Pressable, ScrollView, Text, TextInput, View } from "react-native";

import { ScreenContainer } from "@/components/screen-container";
import { trpc } from "@/lib/trpc";

type GatewayField = "PAYMENT_GATEWAY_ACTIVE_PROVIDER" | "PAYMENT_GATEWAY_PUBLIC_BASE_URL" | "PAYSTACK_SECRET_KEY" | "FLUTTERWAVE_WEBHOOK_SECRET_HASH" | "FLUTTERWAVE_SECRET_KEY";
const fields: Array<{ key: GatewayField; label: string; secret?: boolean; placeholder: string }> = [
  { key: "PAYMENT_GATEWAY_ACTIVE_PROVIDER", label: "Active provider", placeholder: "paystack or flutterwave" },
  { key: "PAYMENT_GATEWAY_PUBLIC_BASE_URL", label: "Public HTTPS origin", placeholder: "https://land.example.gov.ng" },
  { key: "PAYSTACK_SECRET_KEY", label: "Paystack secret key", secret: true, placeholder: "Enter replacement key" },
  { key: "FLUTTERWAVE_WEBHOOK_SECRET_HASH", label: "Flutterwave webhook secret hash", secret: true, placeholder: "Enter replacement hash" },
  { key: "FLUTTERWAVE_SECRET_KEY", label: "Flutterwave secret key", secret: true, placeholder: "Enter replacement key" },
];

export default function PaymentGatewaySettingsScreen() {
  const utils = trpc.useUtils();
  const settings = trpc.integrationSettings.status.useQuery();
  const activation = trpc.paymentOperations.gatewayActivation.useQuery({}, { retry: false });
  const [values, setValues] = useState<Partial<Record<GatewayField, string>>>({});
  const [feedback, setFeedback] = useState<string | null>(null);
  const secureStorageReady = Boolean(settings.data?.secureStorageAvailable);
  const fieldStatus = new Map(settings.data?.fields.map((field) => [field.field, field]));
  const save = trpc.integrationSettings.save.useMutation({
    onSuccess: async () => { setValues({}); setFeedback("Gateway settings saved. Secrets remain server-side and are never shown again."); await Promise.all([utils.integrationSettings.status.invalidate(), utils.paymentOperations.gatewayActivation.invalidate()]); },
    onError: (error) => setFeedback(error.message || "Gateway settings could not be saved."),
  });
  return <ScreenContainer className="bg-background"><ScrollView contentContainerStyle={{ padding: 20, gap: 16 }} keyboardShouldPersistTaps="handled"><View className="rounded-[28px] bg-surface p-5"><Text className="text-sm text-muted">Administrator payment controls</Text><Text className="mt-2 text-3xl font-bold text-foreground">Gateway settings</Text><Text className="mt-2 text-sm leading-5 text-muted">Configure one selected gateway at a time. Values are encrypted on the server when secure storage is available; this screen exposes only configuration state, never stored credentials.</Text></View><View className={`rounded-3xl border p-5 ${activation.data?.ready ? "border-success bg-success/5" : "border-warning bg-warning/5"}`}><Text className={`font-semibold ${activation.data?.ready ? "text-success" : "text-warning"}`}>{activation.data?.ready ? "Callback configuration ready" : "Settlement connection unavailable"}</Text><Text className="mt-2 text-sm leading-5 text-muted">{activation.data?.callbackUrl ? `Register this callback URL in the selected gateway: ${activation.data.callbackUrl}` : activation.data?.reason ?? "Loading activation status…"}</Text><Text className="mt-2 text-xs leading-4 text-muted">A ready configuration does not prove connectivity or settlement. Each successful webhook must still pass provider transaction re-verification.</Text></View>{!secureStorageReady ? <View className="rounded-3xl border border-warning bg-warning/10 p-5"><Text className="font-semibold text-warning">Secure server storage is required</Text><Text className="mt-2 text-sm leading-5 text-muted">{settings.data?.reason ?? "Configure the server encryption key before entering gateway credentials."}</Text></View> : null}<View className="rounded-3xl border border-border bg-surface p-5"><Text className="text-lg font-semibold text-foreground">Credential status and updates</Text><Text className="mt-2 text-sm leading-5 text-muted">Leave a secret field empty to keep its existing encrypted value. Updating either provider’s value does not activate it unless it is selected and its HTTPS callback origin is valid.</Text><View className="mt-4 gap-3">{fields.map((field) => { const status = fieldStatus.get(field.key); return <View key={field.key} className="rounded-2xl border border-border bg-background p-4"><View className="flex-row items-center justify-between gap-3"><Text className="flex-1 text-sm font-semibold text-foreground">{field.label}</Text><Text className={`text-xs font-semibold ${status?.configured ? "text-success" : "text-warning"}`}>{status?.configured ? "Configured" : "Not configured"}</Text></View><Text className="mt-1 text-xs text-muted">{status?.configured ? `Stored via ${status.source.replace(/_/g, " ")}; current value remains redacted.` : "No saved value."}</Text><TextInput value={values[field.key] ?? ""} onChangeText={(value) => setValues((current) => ({ ...current, [field.key]: value }))} editable={secureStorageReady && !save.isPending} secureTextEntry={field.secret} autoCapitalize="none" placeholder={field.placeholder} placeholderTextColor="#94A3B8" className="mt-3 rounded-xl border border-border px-3 py-3 text-sm text-foreground" /></View>; })}</View><Pressable disabled={!secureStorageReady || save.isPending} onPress={() => save.mutate(values)} style={({ pressed }) => [{ opacity: !secureStorageReady || save.isPending ? 0.45 : pressed ? 0.82 : 1 }]}><View className="mt-5 rounded-2xl bg-foreground px-4 py-4">{save.isPending ? <ActivityIndicator color="#FFFFFF" /> : <Text className="text-center font-semibold text-background">Save encrypted gateway settings</Text>}</View></Pressable></View>{feedback ? <View className={`rounded-2xl border p-4 ${save.isError ? "border-error bg-error/5" : "border-primary bg-primary/5"}`}><Text className="text-sm leading-5 text-foreground">{feedback}</Text></View> : null}</ScrollView></ScreenContainer>;
}
