# Copy to .env.falkordb-integration only on a disposable CI/development runner.
# Generate a random value per run; do not commit an actual password.
FALKORDB_PASSWORD=replace_with_a_random_ci_secret
FALKORDB_GRAPH_NAME=ndsep_integration
FALKORDB_QUERY_TIMEOUT_MS=3000
