export type BiometricExportAdapter = { hasHardwareAsync: () => Promise<boolean>; isEnrolledAsync: () => Promise<boolean>; authenticateAsync: (options: { promptMessage: string; promptDescription: string; disableDeviceFallback: boolean; biometricsSecurityLevel: "strong" }) => Promise<{ success: boolean; warning?: string }> };
export async function authorizeEncryptedDiagnosticsExport(adapter: BiometricExportAdapter) { if (!(await adapter.hasHardwareAsync())) return { approved: false, message: "Encrypted support packages require biometric hardware on this device." }; if (!(await adapter.isEnrolledAsync())) return { approved: false, message: "Enroll Face ID or fingerprint before exporting encrypted support packages." }; const result = await adapter.authenticateAsync({ promptMessage: "Confirm encrypted diagnostics export", promptDescription: "Authorize export of the support escalation package.", disableDeviceFallback: true, biometricsSecurityLevel: "strong" }); return result.success ? { approved: true as const, message: "Biometric confirmation approved." } : { approved: false as const, message: result.warning || "Biometric confirmation was not completed." }; }
